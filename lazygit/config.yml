# =============================================================================
# Lazygit Configuration
# =============================================================================
# Symlinked to ~/.config/lazygit/config.yml by install script

git:
  paging:
    colorArg: always
    pager: delta --paging=never
