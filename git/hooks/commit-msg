#!/bin/bash
# Reject commit messages that contain Claude/AI references.
if grep -qiE "(claude|anthropic|generated with claude)" "$1"; then
    echo "ERROR: Commit message contains Claude/AI references."
    echo "Please remove any traces of AI assistance from your commit message."
    exit 1
fi
